## Resubmission

This package was previously on CRAN and was archived on 2026-06-09 because
its dependency `lidR` was archived. `lidR` is back on CRAN (as are `spanner`
and `CrownScorchTLS`, two other Imports that were briefly GitHub-only), so
this resubmission restores the full dependency chain and requests that
tReeTraits be unarchived.

## R CMD check results

0 errors | (see local check log for warnings/notes still being resolved)

## Downstream dependencies

There are currently no downstream dependencies for this package.
