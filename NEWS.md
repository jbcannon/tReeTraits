# tReeTraits 0.1.3

* Resubmission to CRAN. tReeTraits was archived on 2026-06-09 after its
  dependency `lidR` was archived; `lidR`, `spanner`, and `CrownScorchTLS`
  (previously GitHub-only) are all back on CRAN, restoring the full
  dependency chain.
* Regenerated stale `Rd` documentation for `fit_taper_Kozak()` and
  `plot_qsm3d()` so documented arguments match the function signatures.

# tReeTraits 0.1.2

* Initial CRAN release.
